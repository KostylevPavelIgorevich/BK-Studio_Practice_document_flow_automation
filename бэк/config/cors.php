<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Пути, для которых разрешены CORS запросы
    |--------------------------------------------------------------------------
    */

    'paths' => [
        'api/*',
        'login',
        'logout',
        'users',          // ← ДОБАВИТЬ (GET /users)
        'users/*',        // ← уже есть (PUT /users/1, DELETE /users/1)
        'groups',         // ← ДОБАВИТЬ (GET /groups)
        'groups/*',       // ← уже есть (POST /groups, PUT /groups/1, DELETE /groups/1)
        'documents',      // ← ДОБАВИТЬ
        'documents/*',
        'document-types',
        'document-types/*',
        'waybill',
        'waybill/*',
        'sanctum/csrf-cookie',
        'csrf-token',     // ← ДОБАВИТЬ (если используешь эндпоинт для CSRF)
        'test-post',      // ← ДОБАВИТЬ (для тестов)
        'temp',           // ← ДОБАВИТЬ
        'temp/*',
    ],

    /*
    |--------------------------------------------------------------------------
    | Разрешённые методы HTTP
    |--------------------------------------------------------------------------
    */

    'allowed_methods' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Разрешённые источники (origins)
    |--------------------------------------------------------------------------
    */

    'allowed_origins' => [
        'http://localhost:1420',   // Tauri dev
        'http://localhost:1421',   // Tauri dev (альтернативный порт)
        'http://localhost:5173',   // Vite dev
        'http://localhost:3000',   // React dev (альтернатива)
        'https://tauri.localhost', // Tauri production
    ],

    /*
    |--------------------------------------------------------------------------
    | Разрешённые заголовки
    |--------------------------------------------------------------------------
    */

    'allowed_headers' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Разрешённые заголовки ответа
    |--------------------------------------------------------------------------
    */

    'exposed_headers' => [],

    /*
    |--------------------------------------------------------------------------
    | Максимальный возраст кэширования preflight запросов
    |--------------------------------------------------------------------------
    */

    'max_age' => 0,

    /*
    |--------------------------------------------------------------------------
    | Поддержка учетных данных (cookies)
    |--------------------------------------------------------------------------
    | ВАЖНО: Должно быть true для работы с CSRF и сессиями
    */

    'supports_credentials' => true,

];
